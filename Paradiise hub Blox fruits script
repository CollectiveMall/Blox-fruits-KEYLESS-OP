--[[
    ============================================================
    PARADISE HUB – BLOX FRUITS ULTIMATE
    Community Edition v3.0 – Key-Locked
    ============================================================
    - Tab Order: AutoFarm → ESP → Utility → Settings
    - Key System: "OHPARADISENOTAGAIN!" (case-sensitive)
    - Fully featured ESP, AutoFarm placeholder, Utilities, and more.
    - Open-source ready – fork, modify, share.
    - Built with Obsidian Library.
    ============================================================
]]

-- // LOAD OBSIDIAN LIBRARY
local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

-- // SANITY CHECK
if not Library then
    warn("[Paradise Hub] Failed to load Obsidian Library. Check internet connection.")
    return
end

-- // ============================================================
-- // KEY SYSTEM – THE GATEKEEPER
-- // ============================================================
local CORRECT_KEY = "OHPARADISENOTAGAIN!" -- Case-sensitive
local isUnlocked = false

-- // CREATE MAIN WINDOW – PARADISE HUB THEME
local Window = Library:CreateWindow({
    Title = "🔐 Paradise Hub – Locked",
    Footer = "Community Edition v3.0",
    Icon = 95816097006870,
    NotifySide = "Right",
    ShowCustomCursor = true,
})

-- // CREATE TABS (but we'll hide most of them initially)
local Tabs = {
    Key = Window:AddTab("🔑 Unlock", "key"),          -- This is the gatekeeper
    AutoFarm = Window:AddTab("⚔️ AutoFarm", "sword"), -- New order: 1st
    ESP = Window:AddTab("👁️ ESP", "eye"),             -- 2nd
    Utility = Window:AddTab("🧰 Utility", "tool"),    -- 3rd
    Settings = Window:AddTab("⚙️ Settings", "settings"), -- 4th
}

-- // HIDE ALL TABS EXCEPT KEY INITIALLY
local function SetTabsVisibility(visible)
    for tabName, tabObj in pairs(Tabs) do
        if tabName ~= "Key" then
            tabObj.Visible = visible
        end
    end
end
SetTabsVisibility(false) -- Lock everything except Key tab

-- // ============================================================
-- // KEY TAB – UNLOCK INTERFACE
-- // ============================================================
local KeyGroup = Tabs.Key:AddLeftGroupbox("🔑 Enter the Key", "lock")
KeyGroup:AddLabel({
    Text = "This hub is protected.\nEnter the correct key to unlock all features.",
    DoesWrap = true,
})

-- // INPUT BOX FOR KEY
local keyInput = KeyGroup:AddInput("KeyInput", {
    Default = "",
    Numeric = false,
    Finished = true, -- Only trigger on Enter press
    ClearTextOnFocus = false,
    Text = "Enter Key",
    Tooltip = "Type the secret key here",
    Placeholder = "Enter key...",
    Callback = function(Value)
        -- We'll handle verification in a separate function
    end
})

-- // UNLOCK BUTTON
local unlockButton = KeyGroup:AddButton({
    Text = "🔓 Unlock Paradise",
    Func = function()
        local entered = Options.KeyInput.Value or ""
        if entered == CORRECT_KEY then
            isUnlocked = true
            -- Update UI
            Window:SetTitle("🌴 Paradise Hub – Unlocked")
            SetTabsVisibility(true)
            Tabs.Key:SetTitle("✅ Unlocked")
            KeyGroup:AddLabel({
                Text = "✅ Access Granted!\nWelcome to Paradise Hub, fren.",
                DoesWrap = true,
            })
            -- Remove input and button (optional)
            keyInput.Visible = false
            unlockButton.Visible = false
            Library:Notify({
                Title = "🔓 Paradise Unlocked",
                Description = "Full access granted. Enjoy!",
                Time = 4,
            })
        else
            Library:Notify({
                Title = "❌ Access Denied",
                Description = "Incorrect key. Try again, rebel.",
                Time = 3,
                Type = "Error",
            })
            -- Shake the input box (visual feedback)
            keyInput:SetValue("") -- Clear the field
        end
    end,
    DoubleClick = false,
    Tooltip = "Click to verify the key",
})

-- // ============================================================
-- // AUTOFARM TAB (NOW FIRST)
-- // ============================================================
local AutoFarmGroup = Tabs.AutoFarm:AddLeftGroupbox("🌾 AutoFarm Controls", "sword")

-- // AutoFarm Toggle
local farmToggle = AutoFarmGroup:AddToggle("Farm_Toggle", {
    Text = "Enable AutoFarm",
    Tooltip = "Toggle auto-farming on/off",
    Default = false,
    Risky = false,
    Callback = function(Value)
        print("[Paradise Hub] AutoFarm toggled:", Value)
        if Value then
            Library:Notify({
                Title = "🌾 AutoFarm Started",
                Description = "Farming in progress... (placeholder)",
                Time = 3,
            })
        else
            Library:Notify({
                Title = "🌾 AutoFarm Stopped",
                Description = "Farming halted.",
                Time = 2,
            })
        end
    end
})

-- // AutoFarm Settings
AutoFarmGroup:AddSlider("FarmDistance", {
    Text = "Farm Range (studs)",
    Default = 150,
    Min = 50,
    Max = 400,
    Rounding = 0,
    Suffix = " studs",
    Tooltip = "How far to search for enemies",
    Callback = function(Value) end
})

AutoFarmGroup:AddDropdown("FarmTarget", {
    Values = { "NPCs", "Bosses", "Fruits", "Chests" },
    Default = 1,
    Multi = false,
    Text = "Farm Target",
    Tooltip = "What to farm",
    Callback = function(Value) end
})

AutoFarmGroup:AddButton({
    Text = "⚔️ Test Farm (Placeholder)",
    Func = function()
        Library:Notify({
            Title = "⚔️ Farm Test",
            Description = "This feature is coming soon! Stay tuned.",
            Time = 3,
        })
    end,
})

-- // ============================================================
-- // ESP TAB (SECOND – YOUR ORIGINAL REQUEST)
-- // ============================================================
local ESPGroup = Tabs.ESP:AddLeftGroupbox("👁️ ESP Controls", "eye")

-- // ESP Toggle
local espToggle = ESPGroup:AddToggle("ESP_Toggle", {
    Text = "Enable ESP",
    Tooltip = "Turn the ESP system on/off",
    Default = false,
    Risky = false,
    Callback = function(Value)
        print("[Paradise Hub] ESP toggled:", Value)
        if not Value then
            ClearESP()
        end
    end
})

-- // Distance Slider
local distSlider = ESPGroup:AddSlider("DistanceSlider", {
    Text = "Max Distance (studs)",
    Default = 250,
    Min = 50,
    Max = 500,
    Rounding = 0,
    Suffix = " studs",
    Tooltip = "Entities beyond this distance are ignored",
    Callback = function(Value) end
})

-- // Line ESP Toggle
local lineToggle = ESPGroup:AddToggle("LineESP_Toggle", {
    Text = "Draw Lines to Entities",
    Tooltip = "Show a line from your character to the target",
    Default = false,
    Callback = function(Value) end
})

-- // Glow Effect
local glowToggle = ESPGroup:AddToggle("Glow_Toggle", {
    Text = "Glow Effect",
    Tooltip = "Adds a glowing aura around entities",
    Default = true,
    Callback = function(Value) end
})

-- // Color Pickers – Paradise Theme
local playerColor = ESPGroup:AddLabel("Player Color"):AddColorPicker("PlayerColor", {
    Default = Color3.new(0, 0.8, 0.9),    -- Teal
    Title = "Player ESP Color",
    Callback = function(Value) end
})

local npcColor = ESPGroup:AddLabel("NPC Color"):AddColorPicker("NPCColor", {
    Default = Color3.new(1, 0.7, 0.1),    -- Gold
    Title = "NPC ESP Color",
    Callback = function(Value) end
})

local chestColor = ESPGroup:AddLabel("Chest Color"):AddColorPicker("ChestColor", {
    Default = Color3.new(1, 0.4, 0.5),    -- Coral
    Title = "Chest ESP Color",
    Callback = function(Value) end
})

local fruitColor = ESPGroup:AddLabel("Fruit Color"):AddColorPicker("FruitColor", {
    Default = Color3.new(0.8, 0.2, 0.8),  -- Purple
    Title = "Fruit ESP Color",
    Callback = function(Value) end
})

-- // ============================================================
-- // UTILITY TAB (THIRD)
-- // ============================================================
local UtilityGroup = Tabs.Utility:AddLeftGroupbox("🧰 Utility Features", "tool")

-- // Teleport Section
UtilityGroup:AddLabel("🚀 Teleports")
UtilityGroup:AddButton({
    Text = "📍 Teleport to Island (Placeholder)",
    Func = function()
        Library:Notify({
            Title = "🚀 Teleport",
            Description = "Teleport feature coming soon!",
            Time = 3,
        })
    end,
})

UtilityGroup:AddButton({
    Text = "🔄 Reset Character",
    Func = function()
        local player = game.Players.LocalPlayer
        if player and player.Character then
            player.Character:BreakJoints()
            Library:Notify({
                Title = "🔄 Reset",
                Description = "Character reset successfully.",
                Time = 2,
            })
        end
    end,
})

-- // Collect Section
UtilityGroup:AddLabel("📦 Auto-Collect")
UtilityGroup:AddToggle("AutoCollect_Toggle", {
    Text = "Auto-Collect Fruits/Chests",
    Tooltip = "Automatically pick up nearby fruits and chests",
    Default = false,
    Callback = function(Value) end
})

-- // ============================================================
-- // SETTINGS TAB (FOURTH – UI CONFIGURATION)
-- // ============================================================
local SettingsGroup = Tabs.Settings:AddLeftGroupbox("⚙️ UI Settings", "wrench")
SettingsGroup:AddToggle("KeybindMenuOpen", {
    Default = Library.KeybindFrame.Visible,
    Text = "Open Keybind Menu",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})
SettingsGroup:AddToggle("ShowCustomCursor", {
    Text = "Custom Cursor",
    Default = Library.ShowCustomCursor,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})
SettingsGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Notification Side",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})
SettingsGroup:AddSlider("UICornerSlider", {
    Text = "Corner Radius",
    Default = Library.CornerRadius,
    Min = 0,
    Max = 20,
    Rounding = 0,
    Callback = function(value)
        Window:SetCornerRadius(value)
    end
})

-- // ============================================================
-- // ESP ENGINE – THE HEART OF PARADISE HUB (SAME AS BEFORE)
-- // ============================================================

local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Workspace = workspace

-- // STORAGE FOR ESP OBJECTS
local ESPObjects = {
    Highlights = {},
    Billboards = {},
    Lines = {},
    Glows = {},
}

-- // HELPER FUNCTIONS (same as before)
local function CreateBillboard(adornee, text, color, offset)
    local bill = Instance.new("BillboardGui")
    bill.Adornee = adornee
    bill.Size = UDim2.new(0, 200, 0, 40)
    bill.StudsOffset = offset or Vector3.new(0, 3, 0)
    bill.MaxDistance = 500
    bill.AlwaysOnTop = true
    local label = Instance.new("TextLabel", bill)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = color
    label.TextStrokeColor3 = Color3.new(0, 0, 0)
    label.TextStrokeTransparency = 0.3
    label.Font = Enum.Font.GothamBold
    label.TextSize = 14
    label.TextScaled = false
    label.TextWrapped = true
    return bill
end

local function CreateHighlight(adornee, color)
    local hl = Instance.new("Highlight")
    hl.Adornee = adornee
    hl.FillColor = color
    hl.FillTransparency = 0.5
    hl.OutlineColor = Color3.new(1, 1, 1)
    hl.OutlineTransparency = 0.2
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    return hl
end

local function CreateGlow(adornee, color)
    local glowPart = Instance.new("Part")
    glowPart.Anchored = true
    glowPart.CanCollide = false
    glowPart.Material = Enum.Material.Neon
    glowPart.BrickColor = BrickColor.new(color)
    glowPart.Size = Vector3.new(5, 8, 5)
    glowPart.Transparency = 0.7
    glowPart.Shape = Enum.PartType.Cylinder
    glowPart.Parent = adornee
    return glowPart
end

local function CreateLine(targetPos, color)
    local startPos = LocalPlayer.Character and LocalPlayer.Character.PrimaryPart and LocalPlayer.Character.PrimaryPart.Position or Vector3.new(0, 0, 0)
    local midPos = (startPos + targetPos) / 2
    local distance = (startPos - targetPos).Magnitude
    if distance < 1 then return nil end
    local part = Instance.new("Part")
    part.Anchored = true
    part.CanCollide = false
    part.Material = Enum.Material.Neon
    part.BrickColor = BrickColor.new(color)
    part.Size = Vector3.new(0.2, 0.2, distance)
    part.CFrame = CFrame.lookAt(midPos, targetPos) * CFrame.new(0, 0, -distance / 2)
    part.BottomSurface = Enum.SurfaceType.Smooth
    part.TopSurface = Enum.SurfaceType.Smooth
    return part
end

-- // CLEAR ALL ESP
function ClearESP()
    for _, v in pairs(ESPObjects.Highlights) do
        if v and v.Parent then v:Destroy() end
    end
    for _, v in pairs(ESPObjects.Billboards) do
        if v and v.Parent then v:Destroy() end
    end
    for _, v in pairs(ESPObjects.Lines) do
        if v and v.Parent then v:Destroy() end
    end
    for _, v in pairs(ESPObjects.Glows) do
        if v and v.Parent then v:Destroy() end
    end
    ESPObjects.Highlights = {}
    ESPObjects.Billboards = {}
    ESPObjects.Lines = {}
    ESPObjects.Glows = {}
end

-- // MAIN ESP UPDATE LOOP
local function UpdateESP()
    if not Toggles.ESP_Toggle.Value or not isUnlocked then
        ClearESP()
        return
    end

    local maxDist = Options.DistanceSlider.Value or 250
    local showLines = Toggles.LineESP_Toggle.Value or false
    local showGlow = Toggles.Glow_Toggle.Value or true
    local plyColor = Options.PlayerColor.Value or Color3.new(0, 0.8, 0.9)
    local npcColor = Options.NPCColor.Value or Color3.new(1, 0.7, 0.1)
    local chestColor = Options.ChestColor.Value or Color3.new(1, 0.4, 0.5)
    local fruitColor = Options.FruitColor.Value or Color3.new(0.8, 0.2, 0.8)

    ClearESP()

    -- Players
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local char = player.Character
            local root = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
            if root and root.Position then
                local dist = (LocalPlayer.Character and LocalPlayer.Character.PrimaryPart and LocalPlayer.Character.PrimaryPart.Position - root.Position).Magnitude or 999
                if dist <= maxDist then
                    local hl = CreateHighlight(char, plyColor)
                    hl.Parent = char
                    table.insert(ESPObjects.Highlights, hl)
                    local text = player.Name .. " | " .. math.floor(dist) .. "m"
                    local bill = CreateBillboard(root, text, plyColor, Vector3.new(0, 4, 0))
                    bill.Parent = char
                    table.insert(ESPObjects.Billboards, bill)
                    if showGlow then
                        local glow = CreateGlow(root, plyColor)
                        glow.Parent = char
                        table.insert(ESPObjects.Glows, glow)
                    end
                    if showLines then
                        local line = CreateLine(root.Position, plyColor)
                        if line then
                            line.Parent = char
                            table.insert(ESPObjects.Lines, line)
                        end
                    end
                end
            end
        end
    end

    -- NPCs
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
            if not Players:GetPlayerFromCharacter(obj) then
                local root = obj.HumanoidRootPart
                local dist = (LocalPlayer.Character and LocalPlayer.Character.PrimaryPart and LocalPlayer.Character.PrimaryPart.Position - root.Position).Magnitude or 999
                if dist <= maxDist then
                    local npcName = obj.Name or "NPC"
                    local hl = CreateHighlight(obj, npcColor)
                    hl.Parent = obj
                    table.insert(ESPObjects.Highlights, hl)
                    local text = npcName .. " | " .. math.floor(dist) .. "m"
                    local bill = CreateBillboard(root, text, npcColor, Vector3.new(0, 4, 0))
                    bill.Parent = obj
                    table.insert(ESPObjects.Billboards, bill)
                    if showGlow then
                        local glow = CreateGlow(root, npcColor)
                        glow.Parent = obj
                        table.insert(ESPObjects.Glows, glow)
                    end
                    if showLines then
                        local line = CreateLine(root.Position, npcColor)
                        if line then
                            line.Parent = obj
                            table.insert(ESPObjects.Lines, line)
                        end
                    end
                end
            end
        end
    end

    -- Chests
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") or obj:IsA("Part") then
            local name = obj.Name:lower()
            if name:find("chest") or name:find("loot") or name:find("supply") or name:find("money") then
                local root = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Handle") or (obj:IsA("Part") and obj) or obj.PrimaryPart
                if root and root.Position then
                    local dist = (LocalPlayer.Character and LocalPlayer.Character.PrimaryPart and LocalPlayer.Character.PrimaryPart.Position - root.Position).Magnitude or 999
                    if dist <= maxDist then
                        local hl = CreateHighlight(obj, chestColor)
                        hl.Parent = obj
                        table.insert(ESPObjects.Highlights, hl)
                        local text = "📦 Chest | " .. math.floor(dist) .. "m"
                        local bill = CreateBillboard(root, text, chestColor, Vector3.new(0, 2, 0))
                        bill.Parent = obj
                        table.insert(ESPObjects.Billboards, bill)
                        if showGlow then
                            local glow = CreateGlow(root, chestColor)
                            glow.Parent = obj
                            table.insert(ESPObjects.Glows, glow)
                        end
                        if showLines then
                            local line = CreateLine(root.Position, chestColor)
                            if line then
                                line.Parent = obj
                                table.insert(ESPObjects.Lines, line)
                            end
                        end
                    end
                end
            end
        end
    end

    -- Fruits
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Part") or obj:IsA("Model") then
            local name = obj.Name:lower()
            if name:find("fruit") or name:find("devil") then
                local root = obj:FindFirstChild("Handle") or obj:FindFirstChild("HumanoidRootPart") or (obj:IsA("Part") and obj) or obj.PrimaryPart
                if root and root.Position then
                    local dist = (LocalPlayer.Character and LocalPlayer.Character.PrimaryPart and LocalPlayer.Character.PrimaryPart.Position - root.Position).Magnitude or 999
                    if dist <= maxDist then
                        local hl = CreateHighlight(obj, fruitColor)
                        hl.Parent = obj
                        table.insert(ESPObjects.Highlights, hl)
                        local text = "🍎 Fruit | " .. math.floor(dist) .. "m"
                        local bill = CreateBillboard(root, text, fruitColor, Vector3.new(0, 2, 0))
                        bill.Parent = obj
                        table.insert(ESPObjects.Billboards, bill)
                        if showGlow then
                            local glow = CreateGlow(root, fruitColor)
                            glow.Parent = obj
                            table.insert(ESPObjects.Glows, glow)
                        end
                        if showLines then
                            local line = CreateLine(root.Position, fruitColor)
                            if line then
                                line.Parent = obj
                                table.insert(ESPObjects.Lines, line)
                            end
                        end
                    end
                end
            end
        end
    end
end

-- // START THE ESP LOOP (only runs if unlocked)
RunService.Heartbeat:Connect(UpdateESP)

-- // CLEANUP ON CHARACTER CHANGE
LocalPlayer.CharacterAdded:Connect(function()
    ClearESP()
end)

-- // UNLOAD CLEANUP
Library:OnUnload(function()
    ClearESP()
    print("[Paradise Hub] Unloaded. See you in paradise!")
end)

-- // ============================================================
-- // CONFIGURATION SYSTEM
-- // ============================================================
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("ParadiseHub")
SaveManager:SetFolder("ParadiseHub")
SaveManager:SetSubFolder("BloxFruits")
SaveManager:BuildConfigSection(Tabs.Settings)
ThemeManager:ApplyToTab(Tabs.Settings)
SaveManager:LoadAutoloadConfig()

-- // ============================================================
-- // WELCOME NOTIFICATION (only if already unlocked? No, we show key prompt)
-- // ============================================================
Library:Notify({
    Title = "🔐 Paradise Hub – Locked",
    Description = "Enter the key to unlock all features.",
    Time = 5,
})

print("[Paradise Hub] Loaded. Waiting for key: OHPARADISENOTAGAIN!")
