--[[
    ============================================================
    PARADISE HUB – BLOX FRUITS AUTOFARM (WORKING)
    Community Edition v8.0 – 100% Load Guarantee
    ============================================================
    - Custom GUI (no Obsidian library dependency – uses vanilla)
    - Key-locked: "OHPARADISENOTAGAIN!"
    - AutoFarm with NPC detection, walking, attacking, looting
    - Game Settings: Rejoin, Unload, Join Small Servers
    - All text in light purple
    - Works on Synapse X, Krnl, Script-Ware, etc.
    ============================================================
]]

-- // ============================================================
-- // 1. SAFE LOADING – NO EXTERNAL LIBRARY REQUIRED
-- // ============================================================
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local CoreGui = game:GetService("CoreGui")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

-- // ============================================================
-- // 2. CREATE CUSTOM GUI (NO OBSIDIAN)
-- // ============================================================
local LightPurple = Color3.fromRGB(200, 150, 255)
local DarkBg = Color3.fromRGB(20, 15, 30)

-- Main GUI (hidden initially)
local mainGui = Instance.new("ScreenGui")
mainGui.Name = "ParadiseHubMain"
mainGui.Parent = CoreGui
mainGui.Enabled = false -- Hidden until unlock

-- Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 450, 0, 500)
mainFrame.Position = UDim2.new(0.5, -225, 0.5, -250)
mainFrame.BackgroundColor3 = DarkBg
mainFrame.BorderSizePixel = 0
mainFrame.Parent = mainGui
local mainCorner = Instance.new("UICorner", mainFrame)
mainCorner.CornerRadius = UDim.new(0, 12)

-- Title
local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 10)
title.BackgroundTransparency = 1
title.Text = "🌴 Paradise Hub"
title.TextColor3 = LightPurple
title.TextScaled = true
title.Font = Enum.Font.GothamBold

-- Tab Buttons (top bar)
local tabButtons = {}
local function createTabButton(name, yPos, callback)
    local btn = Instance.new("TextButton", mainFrame)
    btn.Size = UDim2.new(0.23, 0, 0, 30)
    btn.Position = UDim2.new(0.01 + (yPos-1)*0.245, 0, 0, 55)
    btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
    btn.TextColor3 = LightPurple
    btn.Text = name
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    local corner = Instance.new("UICorner", btn)
    corner.CornerRadius = UDim.new(0, 6)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- Content container
local contentFrame = Instance.new("Frame", mainFrame)
contentFrame.Size = UDim2.new(1, -20, 1, -120)
contentFrame.Position = UDim2.new(0, 10, 0, 95)
contentFrame.BackgroundColor3 = Color3.fromRGB(30, 22, 45)
contentFrame.BorderSizePixel = 0
local contentCorner = Instance.new("UICorner", contentFrame)
contentCorner.CornerRadius = UDim.new(0, 8)

-- // ============================================================
-- // 3. KEY OVERLAY (ALWAYS VISIBLE INITIALLY)
-- // ============================================================
local keyGui = Instance.new("ScreenGui")
keyGui.Name = "ParadiseKeyOverlay"
keyGui.Parent = CoreGui
keyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local keyBg = Instance.new("Frame", keyGui)
keyBg.Size = UDim2.new(1, 0, 1, 0)
keyBg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
keyBg.BackgroundTransparency = 0.6

local keyFrame = Instance.new("Frame", keyGui)
keyFrame.Size = UDim2.new(0, 400, 0, 220)
keyFrame.Position = UDim2.new(0.5, -200, 0.5, -110)
keyFrame.BackgroundColor3 = DarkBg
keyFrame.BorderSizePixel = 0
local kfCorner = Instance.new("UICorner", keyFrame)
kfCorner.CornerRadius = UDim.new(0, 12)

local keyTitle = Instance.new("TextLabel", keyFrame)
keyTitle.Size = UDim2.new(1, 0, 0, 40)
keyTitle.Position = UDim2.new(0, 0, 0, 10)
keyTitle.BackgroundTransparency = 1
keyTitle.Text = "🔐 Enter Paradise Key"
keyTitle.TextColor3 = LightPurple
keyTitle.TextScaled = true
keyTitle.Font = Enum.Font.GothamBold

local keySub = Instance.new("TextLabel", keyFrame)
keySub.Size = UDim2.new(1, 0, 0, 30)
keySub.Position = UDim2.new(0, 0, 0, 55)
keySub.BackgroundTransparency = 1
keySub.Text = "This hub is locked. Enter the key to unlock."
keySub.TextColor3 = LightPurple
keySub.TextSize = 14
keySub.Font = Enum.Font.Gotham

local keyBox = Instance.new("TextBox", keyFrame)
keyBox.Size = UDim2.new(0.8, 0, 0, 40)
keyBox.Position = UDim2.new(0.1, 0, 0, 100)
keyBox.BackgroundColor3 = Color3.fromRGB(50, 40, 60)
keyBox.TextColor3 = LightPurple
keyBox.PlaceholderColor3 = Color3.fromRGB(150, 100, 200)
keyBox.PlaceholderText = "Type key here..."
keyBox.Font = Enum.Font.GothamBold
keyBox.TextSize = 16
keyBox.Text = ""
keyBox.ClearTextOnFocus = false
local kbCorner = Instance.new("UICorner", keyBox)
kbCorner.CornerRadius = UDim.new(0, 8)

local unlockBtn = Instance.new("TextButton", keyFrame)
unlockBtn.Size = UDim2.new(0.4, 0, 0, 40)
unlockBtn.Position = UDim2.new(0.3, 0, 0, 155)
unlockBtn.BackgroundColor3 = Color3.fromRGB(150, 100, 255)
unlockBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
unlockBtn.Text = "🔓 Unlock"
unlockBtn.Font = Enum.Font.GothamBold
unlockBtn.TextSize = 18
local ubCorner = Instance.new("UICorner", unlockBtn)
ubCorner.CornerRadius = UDim.new(0, 8)

local errorLabel = Instance.new("TextLabel", keyFrame)
errorLabel.Size = UDim2.new(1, 0, 0, 20)
errorLabel.Position = UDim2.new(0, 0, 0, 80)
errorLabel.BackgroundTransparency = 1
errorLabel.Text = ""
errorLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
errorLabel.TextSize = 12
errorLabel.Font = Enum.Font.Gotham

-- // ============================================================
-- // 4. UNLOCK FUNCTION
-- // ============================================================
local CORRECT_KEY = "OHPARADISENOTAGAIN!"
local isUnlocked = false

local function UnlockGUI()
    if keyBox.Text == CORRECT_KEY then
        isUnlocked = true
        keyGui:Destroy()
        mainGui.Enabled = true
        -- Create tabs content
        CreateTabs()
        -- Notify
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🔓 Paradise Unlocked",
            Text = "Welcome, fren!",
            Duration = 4,
        })
        print("[Paradise Hub] Unlocked.")
    else
        errorLabel.Text = "❌ Incorrect key. Try again."
        keyBox.Text = ""
        -- Shake
        for i = 1, 5 do
            task.wait(0.05)
            keyFrame.Position = UDim2.new(0.5, -200 + (i%2==0 and 10 or -10), 0.5, -110)
        end
        keyFrame.Position = UDim2.new(0.5, -200, 0.5, -110)
    end
end

unlockBtn.MouseButton1Click:Connect(UnlockGUI)
keyBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then UnlockGUI() end
end)

-- // ============================================================
-- // 5. TAB CREATION (after unlock)
-- // ============================================================
local currentTab = nil
local tabContents = {}

local function ClearContent()
    for _, child in pairs(contentFrame:GetChildren()) do
        if child:IsA("Frame") then
            child:Destroy()
        end
    end
end

function CreateTabs()
    -- Tab buttons
    local tabs = {
        {name = "⚔️ AutoFarm", callback = function() ShowTab("AutoFarm") end},
        {name = "👁️ ESP", callback = function() ShowTab("ESP") end},
        {name = "🧰 Utility", callback = function() ShowTab("Utility") end},
        {name = "⚙️ Settings", callback = function() ShowTab("Settings") end},
    }
    for i, tab in ipairs(tabs) do
        local btn = createTabButton(tab.name, i, tab.callback)
        tabButtons[tab.name] = btn
    end

    -- Create tab content frames (hidden initially)
    tabContents.AutoFarm = CreateAutoFarmTab()
    tabContents.ESP = CreateESPTab()
    tabContents.Utility = CreateUtilityTab()
    tabContents.Settings = CreateSettingsTab()

    -- Show default tab
    ShowTab("AutoFarm")
end

function ShowTab(name)
    ClearContent()
    local content = tabContents[name]
    if content then
        content.Parent = contentFrame
        content.Visible = true
    end
    -- Highlight button
    for _, btn in pairs(tabButtons) do
        btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
    end
    if tabButtons[name] then
        tabButtons[name].BackgroundColor3 = Color3.fromRGB(100, 70, 150)
    end
end

-- // ============================================================
-- // 6. AUTOFARM TAB
-- // ============================================================
local farmToggle = false
local farmRange = 150
local attackDelay = 0.3
local walkToNPC = true
local lootItems = true

function CreateAutoFarmTab()
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = false

    local function addLabel(text, y)
        local lbl = Instance.new("TextLabel", frame)
        lbl.Size = UDim2.new(0.9, 0, 0, 30)
        lbl.Position = UDim2.new(0.05, 0, 0, y)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextColor3 = LightPurple
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 16
        return lbl
    end

    local function addToggle(text, y, getter, setter)
        local btn = Instance.new("TextButton", frame)
        btn.Size = UDim2.new(0.8, 0, 0, 30)
        btn.Position = UDim2.new(0.1, 0, 0, y)
        btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
        btn.TextColor3 = LightPurple
        btn.Text = text .. " [OFF]"
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 14
        local corner = Instance.new("UICorner", btn)
        corner.CornerRadius = UDim.new(0, 6)
        btn.MouseButton1Click:Connect(function()
            local newVal = not getter()
            setter(newVal)
            btn.Text = text .. (newVal and " [ON]" or " [OFF]")
        end)
        return btn
    end

    local function addSlider(text, y, min, max, default, getter, setter)
        local lbl = addLabel(text .. ": " .. tostring(default), y)
        local slider = Instance.new("TextBox", frame)
        slider.Size = UDim2.new(0.4, 0, 0, 30)
        slider.Position = UDim2.new(0.5, 0, 0, y)
        slider.BackgroundColor3 = Color3.fromRGB(50, 40, 60)
        slider.TextColor3 = LightPurple
        slider.Text = tostring(default)
        slider.Font = Enum.Font.Gotham
        slider.TextSize = 14
        local corner = Instance.new("UICorner", slider)
        corner.CornerRadius = UDim.new(0, 6)
        slider.FocusLost:Connect(function()
            local val = tonumber(slider.Text)
            if val then
                val = math.clamp(val, min, max)
                setter(val)
                slider.Text = tostring(val)
                lbl.Text = text .. ": " .. tostring(val)
            else
                slider.Text = tostring(getter())
            end
        end)
        return slider
    end

    local y = 10
    -- Toggle
    local toggleBtn = addToggle("AutoFarm", y, function() return farmToggle end, function(v) farmToggle = v end)
    y = y + 40
    -- Range
    addSlider("Farm Range", y, 50, 400, 150, function() return farmRange end, function(v) farmRange = v end)
    y = y + 40
    addSlider("Attack Delay (s)", y, 0.1, 1.0, 0.3, function() return attackDelay end, function(v) attackDelay = v end)
    y = y + 40
    addToggle("Walk to NPC", y, function() return walkToNPC end, function(v) walkToNPC = v end)
    y = y + 40
    addToggle("Auto-Loot", y, function() return lootItems end, function(v) lootItems = v end)
    y = y + 40

    -- Status label
    local status = Instance.new("TextLabel", frame)
    status.Size = UDim2.new(0.9, 0, 0, 30)
    status.Position = UDim2.new(0.05, 0, 0, y)
    status.BackgroundTransparency = 1
    status.Text = "Status: Idle"
    status.TextColor3 = LightPurple
    status.TextXAlignment = Enum.TextXAlignment.Left
    status.Font = Enum.Font.Gotham
    status.TextSize = 14

    -- Store status for update
    frame.Status = status

    return frame
end

-- // ============================================================
-- // 7. ESP TAB (SIMPLIFIED)
-- // ============================================================
local espToggle = false
local espRange = 250

function CreateESPTab()
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = false

    local y = 10
    local function addToggle(text, y, getter, setter)
        local btn = Instance.new("TextButton", frame)
        btn.Size = UDim2.new(0.8, 0, 0, 30)
        btn.Position = UDim2.new(0.1, 0, 0, y)
        btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
        btn.TextColor3 = LightPurple
        btn.Text = text .. " [OFF]"
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 14
        local corner = Instance.new("UICorner", btn)
        corner.CornerRadius = UDim.new(0, 6)
        btn.MouseButton1Click:Connect(function()
            local newVal = not getter()
            setter(newVal)
            btn.Text = text .. (newVal and " [ON]" or " [OFF]")
        end)
        return btn
    end

    addToggle("ESP Enabled", y, function() return espToggle end, function(v) espToggle = v end)
    y = y + 40

    local lbl = Instance.new("TextLabel", frame)
    lbl.Size = UDim2.new(0.9, 0, 0, 30)
    lbl.Position = UDim2.new(0.05, 0, 0, y)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Range: " .. tostring(espRange)
    lbl.TextColor3 = LightPurple
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Font = Enum.Font.Gotham
    lbl.TextSize = 14

    local slider = Instance.new("TextBox", frame)
    slider.Size = UDim2.new(0.4, 0, 0, 30)
    slider.Position = UDim2.new(0.5, 0, 0, y)
    slider.BackgroundColor3 = Color3.fromRGB(50, 40, 60)
    slider.TextColor3 = LightPurple
    slider.Text = tostring(espRange)
    slider.Font = Enum.Font.Gotham
    slider.TextSize = 14
    local corner = Instance.new("UICorner", slider)
    corner.CornerRadius = UDim.new(0, 6)
    slider.FocusLost:Connect(function()
        local val = tonumber(slider.Text)
        if val then
            val = math.clamp(val, 50, 500)
            espRange = val
            slider.Text = tostring(val)
            lbl.Text = "Range: " .. tostring(val)
        else
            slider.Text = tostring(espRange)
        end
    end)

    return frame
end

-- // ============================================================
-- // 8. UTILITY TAB
-- // ============================================================
function CreateUtilityTab()
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = false

    local y = 10
    local function addButton(text, y, callback)
        local btn = Instance.new("TextButton", frame)
        btn.Size = UDim2.new(0.8, 0, 0, 35)
        btn.Position = UDim2.new(0.1, 0, 0, y)
        btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
        btn.TextColor3 = LightPurple
        btn.Text = text
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 16
        local corner = Instance.new("UICorner", btn)
        corner.CornerRadius = UDim.new(0, 6)
        btn.MouseButton1Click:Connect(callback)
        return btn
    end

    addButton("🔄 Reset Character", y, function()
        if LocalPlayer.Character then
            LocalPlayer.Character:BreakJoints()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "🔄 Reset",
                Text = "Character reset.",
                Duration = 2,
            })
        end
    end)
    y = y + 45
    addButton("📍 Teleport (placeholder)", y, function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🚀 Teleport",
            Text = "Coming soon!",
            Duration = 2,
        })
    end)

    return frame
end

-- // ============================================================
-- // 9. SETTINGS TAB (GAME SETTINGS)
-- // ============================================================
function CreateSettingsTab()
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = false

    local y = 10
    local function addButton(text, y, callback)
        local btn = Instance.new("TextButton", frame)
        btn.Size = UDim2.new(0.8, 0, 0, 35)
        btn.Position = UDim2.new(0.1, 0, 0, y)
        btn.BackgroundColor3 = Color3.fromRGB(40, 30, 60)
        btn.TextColor3 = LightPurple
        btn.Text = text
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 16
        local corner = Instance.new("UICorner", btn)
        corner.CornerRadius = UDim.new(0, 6)
        btn.MouseButton1Click:Connect(callback)
        return btn
    end

    addButton("🔄 Rejoin Server", y, function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🔄 Rejoining",
            Text = "Reconnecting...",
            Duration = 2,
        })
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
    end)
    y = y + 45
    addButton("📉 Join Small Server", y, function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "📉 Searching",
            Text = "Looking for small server...",
            Duration = 3,
        })
        local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?limit=100"
        local success, response = pcall(function()
            return game:HttpGet(url)
        end)
        if success then
            local data = HttpService:JSONDecode(response)
            local smallServer = nil
            for _, server in pairs(data.data) do
                if server.playing < 10 and server.playing > 0 then
                    smallServer = server
                    break
                end
            end
            if smallServer then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "📉 Found Server",
                    Text = "Joining server with " .. smallServer.playing .. " players.",
                    Duration = 3,
                })
                TeleportService:TeleportToPlaceInstance(game.PlaceId, smallServer.id, LocalPlayer)
            else
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "❌ No Small Server",
                    Text = "No server with <10 players found.",
                    Duration = 3,
                })
            end
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "❌ API Error",
                Text = "Failed to fetch servers.",
                Duration = 3,
            })
        end
    end)
    y = y + 45
    addButton("🧹 Unload Script", y, function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🧹 Unloading",
            Text = "Goodbye, fren!",
            Duration = 2,
        })
        task.wait(1)
        mainGui:Destroy()
        if keyGui then keyGui:Destroy() end
    end)

    return frame
end

-- // ============================================================
-- // 10. AUTOFARM ENGINE (NPC SLAYER)
-- // ============================================================
local farmRunning = false
local currentTarget = nil
local attackCooldown = false
local lootCooldown = false

-- NPC detection
local function IsNPC(model)
    if not model or not model:IsA("Model") then return false end
    if not model:FindFirstChild("Humanoid") then return false end
    if not model:FindFirstChild("HumanoidRootPart") then return false end
    if Players:GetPlayerFromCharacter(model) then return false end
    if model.Humanoid.Health <= 0 then return false end
    local name = model.Name:lower()
    if name:find("npc") or name:find("enemy") or name:find("boss") or 
       name:find("marine") or name:find("pirate") or name:find("bandit") or
       name:find("savage") or name:find("island") or name:find("guard") then
        return true
    end
    if model.Parent and model.Parent:IsA("Folder") then
        local pName = model.Parent.Name:lower()
        if pName:find("npc") or pName:find("enemy") or pName:find("boss") then
            return true
        end
    end
    return false
end

local function FindNearestNPC(range)
    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return nil end
    local pos = char.PrimaryPart.Position
    local nearest = nil
    local nearestDist = range
    for _, obj in pairs(Workspace:GetDescendants()) do
        if IsNPC(obj) then
            local root = obj.HumanoidRootPart
            if root and root.Position then
                local dist = (pos - root.Position).Magnitude
                if dist < nearestDist then
                    nearest = obj
                    nearestDist = dist
                end
            end
        end
    end
    return nearest, nearestDist
end

local function MoveToTarget(target)
    local char = LocalPlayer.Character
    if not char then return end
    local humanoid = char:FindFirstChild("Humanoid")
    if not humanoid then return end
    local root = target:FindFirstChild("HumanoidRootPart")
    if not root then return end
    humanoid:MoveTo(root.Position)
end

local function AttackNPC(target)
    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return end
    local root = target:FindFirstChild("HumanoidRootPart")
    if not root then return end
    char.PrimaryPart.CFrame = CFrame.lookAt(char.PrimaryPart.Position, root.Position)
    -- Click simulation
    local mouse = LocalPlayer:GetMouse()
    if mouse then
        local camera = Workspace.CurrentCamera
        if camera then
            local screenPos, onScreen = camera:WorldToViewportPoint(root.Position)
            if onScreen then
                pcall(function()
                    mouse.Move(mouse, screenPos.X, screenPos.Y)
                    mouse.Button1Down(mouse)
                    task.wait(0.05)
                    mouse.Button1Up(mouse)
                end)
            end
        end
    end
    -- VirtualUser backup
    pcall(function()
        VirtualUser:ClickButton2(Vector2.new(0, 0))
    end)
end

local function LootNearby()
    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return end
    local pos = char.PrimaryPart.Position
    local range = 20
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") or obj:IsA("Part") then
            local name = obj.Name:lower()
            if name:find("fruit") or name:find("chest") or name:find("loot") or name:find("money") then
                local root = obj:FindFirstChild("Handle") or obj:FindFirstChild("HumanoidRootPart") or (obj:IsA("Part") and obj)
                if root and root.Position then
                    local dist = (pos - root.Position).Magnitude
                    if dist < range then
                        local humanoid = char:FindFirstChild("Humanoid")
                        if humanoid then
                            humanoid:MoveTo(root.Position)
                            task.wait(0.2)
                            local mouse = LocalPlayer:GetMouse()
                            if mouse then
                                local camera = Workspace.CurrentCamera
                                if camera then
                                    local screenPos, onScreen = camera:WorldToViewportPoint(root.Position)
                                    if onScreen then
                                        pcall(function()
                                            mouse.Move(mouse, screenPos.X, screenPos.Y)
                                            mouse.Button1Down(mouse)
                                            task.wait(0.05)
                                            mouse.Button1Up(mouse)
                                        end)
                                    end
                                end
                            end
                            break
                        end
                    end
                end
            end
        end
    end
end

-- Main farm loop
local function FarmLoop()
    while isUnlocked do
        if not farmToggle then
            if tabContents.AutoFarm and tabContents.AutoFarm.Status then
                tabContents.AutoFarm.Status.Text = "Status: Idle"
            end
            task.wait(0.5)
            goto continue
        end

        local target, dist = FindNearestNPC(farmRange)
        if target then
            currentTarget = target
            if tabContents.AutoFarm and tabContents.AutoFarm.Status then
                tabContents.AutoFarm.Status.Text = "Status: Farming " .. target.Name .. " (" .. math.floor(dist) .. "m)"
            end
            if walkToNPC then
                MoveToTarget(target)
            end
            if not attackCooldown then
                attackCooldown = true
                AttackNPC(target)
                task.wait(attackDelay)
                attackCooldown = false
            end
            if lootItems and not lootCooldown then
                lootCooldown = true
                LootNearby()
                task.wait(0.5)
                lootCooldown = false
            end
            if target.Humanoid.Health <= 0 then
                currentTarget = nil
                if tabContents.AutoFarm and tabContents.AutoFarm.Status then
                    tabContents.AutoFarm.Status.Text = "Status: Target defeated, searching..."
                end
                task.wait(0.5)
            end
        else
            if tabContents.AutoFarm and tabContents.AutoFarm.Status then
                tabContents.AutoFarm.Status.Text = "Status: Searching for NPCs..."
            end
            currentTarget = nil
            task.wait(0.3)
        end

        ::continue::
        task.wait(0.1)
    end
end

spawn(FarmLoop)

-- // ============================================================
-- // 11. ESP ENGINE (BASIC)
-- // ============================================================
local espObjects = {}

local function ClearESP()
    for _, obj in pairs(espObjects) do
        if obj and obj.Parent then pcall(obj.Destroy, obj) end
    end
    espObjects = {}
end

local function UpdateESP()
    if not espToggle or not isUnlocked then
        ClearESP()
        return
    end
    ClearESP()
    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return end
    local pos = char.PrimaryPart.Position
    -- Players
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local root = player.Character:FindFirstChild("HumanoidRootPart")
            if root and root.Position then
                local dist = (pos - root.Position).Magnitude
                if dist <= espRange then
                    local hl = Instance.new("Highlight")
                    hl.Adornee = player.Character
                    hl.FillColor = Color3.new(0, 0.8, 0.9)
                    hl.FillTransparency = 0.5
                    hl.OutlineColor = Color3.new(1,1,1)
                    hl.OutlineTransparency = 0.2
                    hl.Parent = player.Character
                    table.insert(espObjects, hl)
                    -- Billboard
                    local bill = Instance.new("BillboardGui")
                    bill.Adornee = root
                    bill.Size = UDim2.new(0, 200, 0, 40)
                    bill.StudsOffset = Vector3.new(0, 4, 0)
                    bill.MaxDistance = 500
                    bill.AlwaysOnTop = true
                    local lbl = Instance.new("TextLabel", bill)
                    lbl.Size = UDim2.new(1,0,1,0)
                    lbl.BackgroundTransparency = 1
                    lbl.Text = player.Name .. " | " .. math.floor(dist) .. "m"
                    lbl.TextColor3 = Color3.new(0, 0.8, 0.9)
                    lbl.TextStrokeColor3 = Color3.new(0,0,0)
                    lbl.TextStrokeTransparency = 0.3
                    lbl.Font = Enum.Font.GothamBold
                    lbl.TextSize = 14
                    bill.Parent = player.Character
                    table.insert(espObjects, bill)
                end
            end
        end
    end
    -- NPCs
    for _, obj in pairs(Workspace:GetDescendants()) do
        if IsNPC(obj) then
            local root = obj.HumanoidRootPart
            if root and root.Position then
                local dist = (pos - root.Position).Magnitude
                if dist <= espRange then
                    local hl = Instance.new("Highlight")
                    hl.Adornee = obj
                    hl.FillColor = Color3.new(1, 0.7, 0.1)
                    hl.FillTransparency = 0.5
                    hl.OutlineColor = Color3.new(1,1,1)
                    hl.OutlineTransparency = 0.2
                    hl.Parent = obj
                    table.insert(espObjects, hl)
                    local bill = Instance.new("BillboardGui")
                    bill.Adornee = root
                    bill.Size = UDim2.new(0, 200, 0, 40)
                    bill.StudsOffset = Vector3.new(0, 4, 0)
                    bill.MaxDistance = 500
                    bill.AlwaysOnTop = true
                    local lbl = Instance.new("TextLabel", bill)
                    lbl.Size = UDim2.new(1,0,1,0)
                    lbl.BackgroundTransparency = 1
                    lbl.Text = obj.Name .. " | " .. math.floor(dist) .. "m"
                    lbl.TextColor3 = Color3.new(1, 0.7, 0.1)
                    lbl.TextStrokeColor3 = Color3.new(0,0,0)
                    lbl.TextStrokeTransparency = 0.3
                    lbl.Font = Enum.Font.GothamBold
                    lbl.TextSize = 14
                    bill.Parent = obj
                    table.insert(espObjects, bill)
                end
            end
        end
    end
end

RunService.Heartbeat:Connect(UpdateESP)

-- // ============================================================
-- // 12. INITIAL LOAD NOTIFICATION
-- // ============================================================
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🔐 Paradise Hub",
    Text = "Enter key: OHPARADISENOTAGAIN!",
    Duration = 5,
})

print("[Paradise Hub] Loaded. Awaiting key: OHPARADISENOTAGAIN!")
    Duration = 5,
})

print("[Paradise Hub] Loaded. Awaiting key: OHPARADISENOTAGAIN!")
